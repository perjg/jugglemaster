{
    IBClasses = (
        {
            ACTIONS = {"" = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = InstantTextField; LANGUAGE = ObjC; SUPERCLASS = NSTextField; }, 
        {
            ACTIONS = {
                endExportSheet = id; 
                setPattern = id; 
                setPatternStyle = id; 
                setSpeed = id; 
                setStyle = id; 
                showInspector = id; 
                toggleRecording = id; 
                toggleShowPattern = id; 
            }; 
            CLASS = JMController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                errorReporter = NSImageView; 
                inspectorPanel = NSPanel; 
                movieExportSelection = NSPopUpButton; 
                movieExportView = NSView; 
                patternStyleButtons = NSMatrix; 
                recordingMenuItem = NSMenuItem; 
                styleSelect = NSPopUpButton; 
                view = JMView; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}